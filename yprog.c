#include<stdio.h>
#include<math.h>
void main()
{
    int x,a,b,y,c;
    if(x==1)
    {
        y=pow(a,2) +pow(b,2)+ 2*a*b;
    }
    else if(x==2)
    {
        y=pow(a,4)+pow(c,4)-a*c;
    }
    else if(x==3)
    {
        y=pow((a+5),7);
    }
    else if(x==4)
    {
        y=pow(a,3)+pow(b,3)+3*a*b*c;
    }
}